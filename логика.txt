Пользователь:
	Не может увеличить баланс свой больше чем на 50 000
	Если его баланс меньше чем то, что он хочет потратить - то ошибка
Фонд:
	Если его баланс меньше чем то, что он хочет потратить - то ошибка
Сбор:
	Закрывается по набору необх суммы и остаток возвращается.
	Дата создания устанавливается без участия пользователя


Контроллеры - это логика. Они проверяют, подчиняется ли логике та или иная операция:
	Проконтроллировать правильное увеличение поля philantrops amount в foundrising(через transaction_tab скорре всего)

	По логике может быть только 3 типа транзакций: юзер-фонд, юзер-сбор, фонд-сбор(причем сбор этого фонда).
	Поэтому исключи случаи например: юзер-юзер, фонд-фонд, и тд.

	При донате фонду или сбору проверить, что id получаемого сбора или фонда существует


Транзакцию таки можно удалять

Скопируй базу текущую, чтоб не повредить данные, когда будешь тестить приложуху

Сделать проверку на запрос select - пустой возврат или нет

С опаской следи за //CRINGEEEEEE в foundrising_repository

Проверь, совпадают ли форматы дат в go и postgres

Количество филантропов в foundrising - это только Users, не Foundations

Проверить везде, особенно в репозиториях где селекты - нужно ли выделять размер по массив, 
которые мы объявляем пустым и в него ставим результат селекта. 

В приложухе отображать в транзакциях essence type не как 0 или 1, а словами.

Транзакции удаленных сущностей должны удаляться

Админ - логин ADMIN, пароль - ADMIN

должна быть волюта для каждой страны своя !!!! - добавить валюту в каждую таблицу, где есть деньги. И интерфейс тоже должен быть с деньгами(например когда пополняешь баланс)

В FOUNDRISING где дата закрытия сбора, когда он NULL - сделать, чтобы было как строка "Еще не закрыт"

При обновление Foundrising Админом - проверять, если новая сумма фонда меньше той, что уже собрана - то закрыть( я просто сделаю так, что нельзя обновлять сумму меньше той, что установлена)

Если ввод остался пустым - то оставить поле без изменений в обновлении

Многострочные запросы сортируй по ID

в Foundrising Service в create found_id проверяй на существование

Когда мы добавляем сбор, у foundation поле curfingamount должно увеличиваться на 1

Не должен быть ID отрицательным.

При донате должна иметься возможность оставлять коммент 

Где в репах возвращаются массивы - проверить, чтобы массив был не пустой(то есть запрос нашелся)


Триггеры:
	Сделать логи просто : log_update, log_create, log_delete, log_insert 

При изменении логина проверка, чтобы новый не совпадал со старым и с другими логинами

Остаток от средств, пересланных сбору будет направлен фонду, и это будет отдельная транзакция

Страничка юзера после транзакций обновляется


Юзеру только активные сборы попадаются

Сделать проверку на входе, чтобы было : повторите попытку